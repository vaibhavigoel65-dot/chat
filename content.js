// content.js
// Injected into gem.gov.in pages — no credentials accessed, read-only DOM access only.
// This file intentionally minimal; all logic is in popup.js via executeScript.
console.log('[GEM Exporter] Content script loaded on:', window.location.href);
